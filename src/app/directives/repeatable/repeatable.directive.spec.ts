import { RepeatableDirective } from './repeatable.directive';

describe('RepeatableDirective', () => {
  it('should create an instance', () => {
    const directive = new RepeatableDirective();
    expect(directive).toBeTruthy();
  });
});
